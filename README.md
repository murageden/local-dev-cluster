# local dev cluster

This repository keeps track of my `terraform (.tf)` and `YAML` configurations for my test environment Kubernetes Cluster

# Requirements

- Kubernetes Cluster
- HCP Terraform Installed